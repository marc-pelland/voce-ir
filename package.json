{
  "name": "voce-ir-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "Voce IR workspace root — not published. Real packages live under packages/*.",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0"
}
