[package]
name = "voce-compiler-hybrid"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Voce IR hybrid compiler — per-component target selection across DOM, WebGPU, and WASM"

[dependencies]
voce-schema = { path = "../schema" }
voce-compiler-dom = { path = "../compiler-dom" }
voce-compiler-webgpu = { path = "../compiler-webgpu" }
voce-compiler-wasm = { path = "../compiler-wasm" }
anyhow.workspace = true
serde.workspace = true
serde_json.workspace = true

[dev-dependencies]
insta.workspace = true
