{
  "name": "@voce-ir/mcp-server",
  "version": "0.3.0",
  "description": "Voce IR MCP server — exposes IR tools for Claude Code and MCP clients",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "voce-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.4"
  }
}
